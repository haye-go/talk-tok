import { AdminPage } from "@/pages/admin-page";

export function AdminModelsPage() {
  return (
    <AdminPage
      title="Providers & Models"
      description="Model registry, pricing, feature assignment, and JSON overrides placeholder."
    />
  );
}
