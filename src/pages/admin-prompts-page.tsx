import { AdminPage } from "@/pages/admin-page";

export function AdminPromptsPage() {
  return (
    <AdminPage
      title="Prompt Templates"
      description="Editable prompt defaults, variables, validation, and versioning placeholder."
    />
  );
}
